name: ForenseNet Daily Pipeline

on:
  # schedule:
  #   - cron: '0 3 * * *' # DESATIVADO PARA PARAR EMAILS DE FALHA
  workflow_dispatch: # Permite rodar manualmente se precisar

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Workflow desativado até ajustar ambiente de produção."
